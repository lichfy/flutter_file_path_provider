package tech.fiissh.flutter.plugin.provider.example;

import io.flutter.app.FlutterApplication;

public class DefaultApplication extends FlutterApplication {
    @Override
    public void onCreate() {
        super.onCreate();
    }
}
